---
name: semantic-discovery
description: "Use when turning bounded evidence and adaptive interviews into verified semantic-layer proposals. This is a development forwarder; the canonical body ships with the selayer-discovery package."
---

# Semantic Discovery (repository forwarder)

The canonical skill body is maintained inside the `selayer-discovery` companion
package so the workflow text has a single source of truth. Read it there before
acting:

`packages/selayer-discovery/skills/semantic-discovery/SKILL.md`

This file is the repository development entry point. It intentionally does not
duplicate the workflow body — follow the canonical file above for the command
flow, hard rules, and error behavior.

In a packaged install, the same canonical file ships inside the
`selayer-discovery` wheel under `skills/semantic-discovery/SKILL.md`.
